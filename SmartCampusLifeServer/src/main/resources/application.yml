server:
  address: 0.0.0.0
  port: 8080
  servlet:
    context-path: /v1

spring:
  application:
    name: SmartCampusLifeServer
  datasource:
    url: jdbc:mysql://localhost:3306/campus?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Housizhe3534
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: SmartCampusLifeServerJwtSecretKey2026PleaseChangeInProduction

app:
  health:
    abnormal-checkin-threshold: 50
  report:
    default-exercise-target: 150
    procrastination-threshold: 70
  upload:
    dir: uploads
